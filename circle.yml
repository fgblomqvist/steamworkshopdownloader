machine:
  services:
      - docker

dependencies:
  override:
      - sudo pip install -U docker-compose
      
test:
    override:
        - docker-compose build web
        - docker-compose run --no-deps web nosetests -w /code
