machine:
  services:
      - docker
  python:
    version: 3.4

dependencies:
  override:
      - sudo pip install -U docker-compose
      
test:
    override:
        - docker-compose run -d --no-deps swd
        - docker run swd nosetests
